#include "core/derived_metrics.h"

float offensivePower(const PlayerTotals& pt,
                     std::string_view className) {
    (void)pt;
    (void)className;
    return 0.f;
}

float defensiveScore(const PlayerTotals& pt,
                     std::string_view className) {
    (void)pt;
    (void)className;
    return 0.f;
}
